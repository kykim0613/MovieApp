import { DefaultTheme } from "styled-components";

export const darkMode = {
    bgColor: "#111",
    textColor: "#fff",
    accentColor: "#5078dc",
    btnColor: "#fff",
    btnAccentColor: "#444"
}

export const lightMode = {
    bgColor: "#fff",
    textColor: "#111",
    accentColor: "#5078dc",
    btnColor: "#fff",
    btnAccentColor:"#444"
}